class Solution {
    public int findKthPositive(int[] arr, int k) {
        
        // int ans[]=new int[arr.length];

        for(int i=0;i<arr.length;i++){

            if(arr[i]<=k){
                k++;
            }
        }
        return k;
    }
}